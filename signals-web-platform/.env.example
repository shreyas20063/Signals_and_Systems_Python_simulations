# Signals & Systems Web Platform - Environment Configuration
# Copy this file to .env and configure for your environment

# ============================================================================
# Backend Configuration
# ============================================================================

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Cache configuration
CACHE_MAX_SIZE=10000
CACHE_TTL_SECONDS=300

# Rate limiting (requests per minute per IP)
RATE_LIMIT_PER_MINUTE=100

# Database URL (SQLite for development, PostgreSQL for production)
DATABASE_URL=sqlite:///./simulations.db

# CORS allowed origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ============================================================================
# Frontend Configuration
# ============================================================================

# Backend API URL
VITE_API_URL=http://localhost:8000

# WebSocket URL (leave empty to auto-detect from API URL)
VITE_WS_URL=

# ============================================================================
# Production Only (use GitHub Secrets in CI/CD)
# ============================================================================

# Docker Hub credentials (for CI/CD)
# DOCKER_USERNAME=your-dockerhub-username
# DOCKER_PASSWORD=your-dockerhub-token

# Cloud provider deployment hooks
# RENDER_DEPLOY_HOOK=https://api.render.com/deploy/...
# DO_APP_ID=your-digitalocean-app-id

# Monitoring (optional)
# SENTRY_DSN=https://xxx@sentry.io/xxx
# UPTIME_ROBOT_API_KEY=xxx
